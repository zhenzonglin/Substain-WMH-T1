# 变更记录

## V1.0.9 — 2026-08-31

- WMH与原生FLAIR 20区图谱要求shape完全一致，并以8个对应角点最大世界坐标位移`≤0.05 mm`判断同一网格。
- 网格真实不匹配状态新增shape、仿射最大系数差和最大角点位移诊断，不修改NIfTI头也不重采样。
- 所有阶段成功和失败状态新增开始、结束、耗时及当前/子进程峰值RSS记录。
- 失败病例继续只保留`status/`与`logs/`，不保留影像中间文件。
- 新增工作站1dry-run重排队、3例先验验证、200例固定波次、独立启动/停止和三视图30秒监测工具。
- 运行参数保持每例4线程、200例波次、96 cores和GPU 0；内存异常只告警，不自动限流。

## V1.0 — 2026-08-23

- 同时支持标准BIDS与递归文件夹输入，生成无session的BIDS软链接视图。
- 病灶输入固定为FSL MNI152标准1 mm或2 mm严格二值掩膜。
- 病灶按`MNI152 → ch2better → T1 → FLAIR`链转换，始终使用最近邻标签插值。
- 主流程固定使用SynthStrip、T1–FLAIR刚体配准和模板空间自动对侧WMH替代。
- QC缩减为四图，新增可中断、可恢复、哈希失效保护的本地网页审核程序。
- 正式40维表只接收人工QC通过病例；计算完成暂存表与正式主矩阵分离。
- 成功病例自动删除可重建大体积中间件，失败病例保留诊断文件。
- 总任务并发上限设为200，GPU自动检测并执行每张卡一次一个任务。
- 项目Python包版本升级为`1.0.0`。
